# RollingThunder Deployment (Subsystem)

This directory contains the *mechanics* of deployment.

Authoritative deployment rules, ownership boundaries, and rationale
are defined in:

  docs/DEPLOYMENT.md

Scripts in this directory MUST enforce those rules.
If a script contradicts docs/DEPLOYMENT.md, the script is wrong.

This is intentional.
